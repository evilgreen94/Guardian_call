# Google AI Studio API key (https://aistudio.google.com/apikey)
GOOGLE_API_KEY=

# FALSE = use the direct Gemini API with GOOGLE_API_KEY above.
# TRUE = use Vertex AI instead (needs GOOGLE_CLOUD_PROJECT set too) — for the later Cloud deployment phase.
GOOGLE_GENAI_USE_VERTEXAI=FALSE
